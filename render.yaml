services:
  - type: web
    name: kaustav-portfolio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GMAIL_ID
        value: kdas.portfolio@gmail.com
      - key: ADMIN_USERNAME
        value: kaustavdas1703
      - key: PORTFOLIO_NAME
        value: Kaustav Das
      - key: DATABASE_URI
        value: sqlite:///MyPortfolio.db
    # Note: Add GMAIL_PASSWORD and ADMIN_PASSWORD manually in Render dashboard for security
